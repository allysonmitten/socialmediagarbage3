<div class="container" ng-controller="navController">
    <% if (user != undefined && user.local.email != "") { %>
    <div ng-init="isLoggedIn = true">
    <% } else { %>
    <div ng-init="isLoggedIn = false">
    <% } %>

    <div class="row">
        <div class="col-md-6">
            <a href="/"><h2 id="navLogo"><img class="navImg" src="/assets/images/logo2.png">HEAVY GARBAGE TINDER</h2></a></img>
        </div>
        <div class="row top-buffer">
                <div class="col-md-offset-6 col-md-1 top-buffer1" id="navLinks" ng-show="isLoggedIn">
                    <a href="/post/new" style="margin-top: -10px">POST</a>
                </div>
                <div class="col-md-1 top-buffer1" id="navLinks" ng-hide="isLoggedIn">
                    <a href="#" data-toggle="modal" data-target="#signin" style="margin-top: -10px">POST</a>
                </div>
                <div class="col-md-1 top-buffer1" id="navLinks">
                    <h1 style="margin-top: -10px">|</h1>    
                </div> 
                <div class="col-md-1 top-buffer1" id="navLinks2">
                    <a href="/browse">BROWSE</a>
                </div>
                 <div class="col-md-1 top-buffer1" id="navLinks">
                    <h1 style="margin-top: -10px">|</h1>    
                </div> 
                <div class="col-md-1 top-buffer1" id="navLinks2" ng-hide="isLoggedIn">
                    <a href="#" data-toggle="modal" data-target="#signin">LOGIN</a>    
                </div> 
                <div class="col-md-1 top-buffer1" id="navLinks2" ng-show="isLoggedIn">
                    <a href="/logout">LOGOUT</a>    
                </div> 
        </div> 
         

    </div>
</div>